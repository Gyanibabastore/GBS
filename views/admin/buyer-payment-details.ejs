<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Individual Buyer Payment Details - Gyanibaba Store</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/admin/buyer-payment-details.css">
</head>

<body>
  <div class="container">
    <h2 class="mb-4 text-warning">📤 Payment Given to Buyer - <%= buyer.name %></h2>
   
    <!-- Buyer Info -->
    <div class="mb-4">
      <h5>Buyer Information</h5>
      <p>
        Name: <strong><%= buyer.name %></strong><br>
        Mobile: <strong><%= buyer.mobile %></strong><br>
        Email: <strong><%= buyer.email %></strong><br>
        UPI ID: <strong><%= buyer.upi%></strong>
      </p>
      <!-- Add Payment Button -->
<!-- Add Payment Button (GET with buyerId) -->
<div class="mb-4 text-end">
  <a href="/admin/addpayment?buyerId=<%= buyer._id %>" class="btn btn-primary">
    ➕ Add Payment to Buyer
  </a>
</div>

    </div>
    <!-- Summary -->
    <div class="row mb-4">
      <div class="col-md-6">
        <div class="summary-card bg-success">
          <h5>Total Paid</h5>
          <p class="h4">₹<%= totalPayment.toLocaleString() %></p>
        </div>
      </div>
      <div class="col-md-6">
        <div class="summary-card">
          <h5>Total Due to Buyer</h5>
<p class="h4 ">₹<%= totalDue %></p>
        </div>
      </div>
    </div>

    
    <!-- Payment Transactions -->
    <div class="row g-4">
      <% payments.forEach((txn, index) => { %>
        <div class="col-md-6">
          <div class="card p-3">
            <img src="<%= txn.image %>" class="transaction-img mb-3" alt="Proof">
            <div class="card-body">
              <h5 class="card-title">Transaction <%= index + 1 %></h5>
              <p class="card-text">
                Amount: ₹<%= txn.amount %><br>
                Mode: <%= txn.mode %><br>
                Date: <%= txn.date.toISOString().split('T')[0] %>
              </p>
            </div>
          </div>
        </div>
      <% }); %>
    </div>

  </div>
  <script src="/js/admin/buyer-payment-details.js"></script>
</body>

</html>
