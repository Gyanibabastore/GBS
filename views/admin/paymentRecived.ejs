<!DOCTYPE html>
<html lang="en">
<head>
  <<%- include('../partials/head', { title: "Gyanibaba Store Dashboard" }) %>
  <link rel="stylesheet" href="/css/admin/paymentRecived.css" />
</head>

<body>
  <!-- ✅ Admin Navbar Include -->
  <%- include('../partials/adminNavbar') %>
  <div class="container">
    <a href="/admin/dashboard" class="btn btn-warning back-btn">⬅ Back to Dashboard</a>
    <br>
    <h2 class="mb-4 text-primary">💵 Payment Received Summary</h2>

    <!-- Filter Section -->
    <div class="row filter-section">
      <div class="col-md-4 mb-3">
        <label for="filterDate" class="form-label">Select Date</label>
        <input type="date" class="form-control" id="filterDate" value="<%= filterDate || '' %>">
      </div>
      <div class="col-md-4 mb-3">
        <label for="filterMonth" class="form-label">Select Month</label>
        <select id="filterMonth" class="form-select">
          <% months.forEach(month => { %>
            <option value="<%= month %>" <%= month === filterMonth ? 'selected' : '' %>><%= month %></option>
          <% }) %>
        </select>
      </div>
    </div>

    <!-- Summary Card -->
    <div class="row">
      <div class="col-md-12">
        <div class="summary-card text-center">
          <h5>Total Payment Received</h5>
          <p class="display-6 fw-bold text-danger">₹<%= totalAdvance.toLocaleString() %></p>
        </div>
      </div>
    </div>

    <!-- Payment Cards -->
    <div class="row g-4">
      <% payments.forEach(payment => { %>
        <div class="col-md-6">
          <div class="card p-3">
            <img src="<%= payment.proofImg || 'https://source.unsplash.com/400x200/?money,transfer' %>" class="transaction-img mb-3" alt="Proof">
            <div class="card-body">
              <h5 class="card-title"><%= payment.name %></h5>
              <p class="card-text">
                📱 <%= payment.phone %><br />
                💰 ₹<%= payment.amount.toLocaleString() %><br />
                🧾 <%= payment.mode %><br />
                📅 <%= payment.date %>
              </p>
            </div>
          </div>
        </div>
      <% }) %>
    </div>
  </div>

  <script src="/js/admin/paymentRecived.js"></script>
</body>
</html>
