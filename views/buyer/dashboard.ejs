<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('../partials/head', { title: "Gyanibaba Store Dashboard" }) %>
  <link href="/css/buyer/dashboard.css" rel="stylesheet" />
</head>
<body>

  <%- include('../partials/navbar') %>

  <div class="container py-5">
    <div class="overlay">
      <h2 class="mb-5">👤 <%= dashboardData.name %></h2>

      <!-- Summary Section -->
      <div class="row section">

        <!-- 🎁 Deals & Offers -->
        <div class="col-md-3">
          <a href="/buyer/deals" class="dashboard-card card p-4 text-center text-decoration-none text-dark d-block" data-sticker="🎁">
            
            <h5 class="card-title">🎁 Deals & Offers</h5>
            <p class="value">Don’t Miss Out!</p>
          </a>
        </div>

        <!-- 🛠 Manage Orders -->
        <div class="col-md-3">
          <a href="/buyer/manage-orders/<%= dashboardData._id %>" class="dashboard-card card p-4 text-center text-decoration-none text-dark d-block" data-sticker="🛠">
            <h5 class="card-title">🛠 Manage Orders</h5>
            <p class="value">update</p>
          </a>
        </div>

        <!-- 📦 My Orders -->
        <div class="col-md-3">
          <a href="/buyer/<%= dashboardData._id %>/orders" class="dashboard-card card p-4 text-center text-decoration-none text-dark d-block" data-sticker="📦">
            <h5 class="card-title">📦 My Orders</h5>
            <p class="value"><%= dashboardData.deliveredOrdersCount.toLocaleString() %></p>
          </a>
        </div>

        <!-- 🚚 Out For Delivery -->
        <div class="col-md-3">
          <a href="/buyer/out-for-delivery" class="dashboard-card card p-4 text-center text-decoration-none text-dark d-block" data-sticker="🚚">
            <h5 class="card-title">🚚 Out For Delivery</h5>
            <p class="value text-warning"><%= dashboardData.pendingOrdersCount.toLocaleString() %></p>
          </a>
        </div>

        <!-- 💳 Due Payment -->
        <div class="col-md-3 mt-4">
          <a href="/buyer/payment-history" class="dashboard-card card p-4 text-center text-decoration-none text-dark d-block" data-sticker="💳">
            <h5 class="card-title">📉 Due Payment</h5>
            <p class="value text-danger">₹<%= dashboardData.totalDue.toLocaleString() %></p>
          </a>
        </div>

        <!-- 💰 My Earnings -->
        <div class="col-md-3 mt-4">
          <a href="/buyer/dashboard/<%= dashboardData._id %>" class="dashboard-card card p-4 text-center text-decoration-none text-dark d-block" data-sticker="💸">
            <h5 class="card-title">💰 My Earnings</h5>
            <p class="value text-success">₹<%= dashboardData.totalEarning.toLocaleString() %></p>
          </a>
        </div>

        <!-- 📜 Payment History -->
        <div class="col-md-3 mt-4">
          <a href="/buyer/payment-history" class="dashboard-card card p-4 text-center text-decoration-none text-dark d-block" data-sticker="📜">
            <h5 class="card-title">📜 Payment History</h5>
            <p class="value">View Records</p>
          </a>
        </div>

      </div>

    </div>
  </div>
   <%- include('../partials/footer', ) %>
</body>
</html>
