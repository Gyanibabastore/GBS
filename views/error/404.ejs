<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>404 - Page Not Found</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: radial-gradient(circle at top left, #ff416c, #ff4b2b);
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      color: white;
      font-family: 'Segoe UI', sans-serif;
      overflow: hidden;
    }
    .error-box {
      text-align: center;
      animation: fadeIn 1s ease-in-out;
    }
    .error-code {
      font-size: 120px;
      font-weight: 800;
    }
    .message {
      font-size: 22px;
      margin-bottom: 30px;
    }
    .btn-home {
      background: #fff;
      color: #ff4b2b;
      font-weight: 600;
      border-radius: 25px;
      padding: 10px 25px;
      transition: all 0.3s;
    }
    .btn-home:hover {
      background: #ffd6d6;
    }
    @keyframes fadeIn {
      from { opacity: 0; transform: scale(0.95); }
      to { opacity: 1; transform: scale(1); }
    }
  </style>
</head>
<body>
  <div class="error-box">
    <div class="error-code">404</div>
    <div class="message"><%= typeof msg !== 'undefined' ? msg : 'Oops! Page not found.' %></div>
    <a href="/" class="btn btn-home">Back to Home</a>
  </div>
</body>
</html>

